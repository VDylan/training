let question1 = prompt(`What is the code below going to output ? alert( null || 2 || undefined )`, ``);

if (question1 == 2) {
    alert('True because || always search the truthy value');
} else {
    alert(False);
};